# zsh shebang
#
# define a OUTPUT_DIR var
#
# parse zsh arg: container name
#
# drfine a func called log that prints messges only if --verbose is passed else nothing
#
# for that docker container find the dict of its volume names: path
#
# for each of the volumes create a tar backup  compressed into OUTPUT_DIR / volume name / tarfile with date
#
# use log func throughout
